services:
  - type: web
    name: lynn-music
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python api_server.py
    envVars:
      - key: PORT
        value: 10000
    plan: free
